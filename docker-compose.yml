services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: meurebanho
      POSTGRES_PASSWORD: meurebanho123
      POSTGRES_DB: meurebanho_dev
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
